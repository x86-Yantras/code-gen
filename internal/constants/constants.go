package constants

const TemplatesDir = "templates"
const ConfigDir = "./config"
const TemplateExtension = ".tmpl"

// Messages
const ProjectBuiltMsg = `Built %s project, don't forget to commit`
const UndefinedCommandMsg = `Cannot parse command %s`
